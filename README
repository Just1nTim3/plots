Install:
python and add to path
add pip directory to path
install matplotlib
    py -m pip install -U matplotlib
install pandas
    py -m pip install -U pandas


How to run:
1. Add CSV files to /data directory
2. Open terminal or press "terminal -> new terminal" (VS Code)
3. Use "py plot.py"
